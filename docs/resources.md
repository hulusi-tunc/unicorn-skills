# Team resources

Tools, sites, and references the team trusts. Claude checks this list when a task
needs an external resource and suggests one **only if it genuinely fits the need** —
not by default. Add a row when someone shares something worth keeping; include the
*use when* so the suggestion is targeted, not generic.

| Resource | What it is | Use when | Added by |
|---|---|---|---|
| _(example)_ Mobbin | Real mobile/web UI screenshot library | You need real-world screen references for a pattern (onboarding, paywall, settings) | — |

<!-- Add real rows above this line. Keep "use when" specific. Drop anything the team stops reaching for. -->
